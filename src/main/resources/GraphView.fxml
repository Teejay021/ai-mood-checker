<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.layout.*?>
<?import javafx.scene.control.*?>
<?import javafx.geometry.Insets?>

<VBox xmlns="http://javafx.com/javafx/17"
      xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="com.aimoodchecker.controller.GraphController"
      spacing="24" 
      styleClass="glass-card"
      style="-fx-padding: 32;">

    <HBox spacing="16" alignment="CENTER_LEFT">
        <Button text="⬅️ Back" 
                onAction="#onBack" 
                styleClass="modern-button secondary-button"/>
        <Label text="📊 Mood Trends" 
               styleClass="title-label" 
               style="-fx-font-size: 24px;"/>
        <Pane HBox.hgrow="ALWAYS" />
        <Label text="Range:" 
               styleClass="modern-label"/>
        <ComboBox fx:id="rangeCombo" 
                  prefWidth="140"
                  styleClass="modern-combo"/>
        <Button text="🔄 Refresh" 
                onAction="#onRefresh" 
                styleClass="modern-button info-button"/>
    </HBox>

    <!-- Status label for feedback -->
    <Label fx:id="statusLabel" 
           text="Ready to display trends" 
           styleClass="status-label info"
           alignment="CENTER" />

    <!-- Chart container -->
    <VBox fx:id="chartContainer" 
           VBox.vgrow="ALWAYS" 
           styleClass="trend-chart"
           style="-fx-padding: 24;" />

    <Label text="💡 Tip: Both your mood and AI sentiment are displayed on the same 0-1 scale for easy comparison."
           styleClass="modern-label"
           style="-fx-font-size: 14px; -fx-opacity: 0.8;"
           alignment="CENTER" />

</VBox>